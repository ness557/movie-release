spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

telegram:
  bot: movie_notify_bot
  id: movie_notify_bot
  name: nothing_to_look_for_here
  token: tokenn
  webInterfaceLink: movie-release.ddns.net